import turtle as t #turtle 라이브러리를 가져온다.

side = 100  # 변의 길이
angle = 90  # 회전할 각도

t.fd(side)  # 변수 side에 지정된 만큼 선을 긋는다.
t.lt(angle) # 변수 angle에 지정된 만큼 왼쪽으로 회전한다.
t.fd(side)  # 변수 side에 지정된 만큼 선을 긋는다.
t.lt(angle) # 변수 angle에 지정된 만큼 왼쪽으로 회전한다.
t.fd(side)  # 변수 side에 지정된 만큼 선을 긋는다.
t.lt(angle) # 변수 angle에 지정된 만큼 왼쪽으로 회전한다.
t.fd(side)  # 변수 side에 지정된 만큼 선을 긋는다.
t.lt(angle) # 변수 angle에 지정된 만큼 왼쪽으로 회전한다.
